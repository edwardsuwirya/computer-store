import { TabNewRowDirective } from './tab-new-row.directive';

describe('TabNewRowDirective', () => {
  it('should create an instance', () => {
    const directive = new TabNewRowDirective();
    expect(directive).toBeTruthy();
  });
});
