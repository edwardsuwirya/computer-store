import { TabDelRowDirective } from './tab-del-row.directive';

describe('TabDelRowDirective', () => {
  it('should create an instance', () => {
    const directive = new TabDelRowDirective();
    expect(directive).toBeTruthy();
  });
});
