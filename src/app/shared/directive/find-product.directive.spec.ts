import { FindProductDirective } from './find-product.directive';

describe('FindProductDirective', () => {
  it('should create an instance', () => {
    const directive = new FindProductDirective();
    expect(directive).toBeTruthy();
  });
});
