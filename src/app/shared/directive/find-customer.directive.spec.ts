import { FindCustomerDirective } from './find-customer.directive';

describe('FindCustomerDirective', () => {
  it('should create an instance', () => {
    const directive = new FindCustomerDirective();
    expect(directive).toBeTruthy();
  });
});
